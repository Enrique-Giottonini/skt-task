server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Enrique-Giottonini/stk-task-config-server.git
#          uri: file://${user.home}/Code/java/stk-task-config-server
          default-label: main